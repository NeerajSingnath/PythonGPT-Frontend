# Local Vite development only.
# Production Docker builds intentionally do not copy frontend/.env.
# In production, PythonGPT uses same-origin HTTP and WebSocket routing through Nginx.

VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_WS_BASE_URL=ws://127.0.0.1:8000
